{
  "name": "petpawsphere-functions",
  "version": "1.0.0",
  "private": true,
  "main": "lib/index.js",
  "engines": { "node": "22" },
  "scripts": {
    "build": "node esbuild.mjs",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "drizzle-orm": "^0.39.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0",
    "postgres": "^3.4.4",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.3",
    "firebase-functions-test": "^3.1.0"
  }
}
